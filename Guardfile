# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard "jekyll-plus", :config => ['_config.yml', '_guard.yml'], :extensions => ['htm', 'css', 'js'], :serve => true do
  watch /.*/
  ignore /^_site/
  ignore /search.json/
end
